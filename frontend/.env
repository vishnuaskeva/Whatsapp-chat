# Leave empty to auto-detect from window.location on the network
# This allows the app to work from any IP (localhost, 192.168.x.x, etc.)
# 
# For specific IP, uncomment and replace MACHINE_IP with your actual IP:
# VITE_SOCKET_URL=http://192.168.1.100:5000
# VITE_API_URL=http://192.168.1.100:5000/api

# Auto-detection enabled (empty = use window.location)
VITE_SOCKET_URL=
VITE_API_URL=
